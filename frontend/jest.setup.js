// Minimal Jest setup file
// Add global test setup here (e.g., extend-expect) if testing libraries are installed

// Example if using @testing-library/jest-dom:
// try { require('@testing-library/jest-dom/extend-expect'); } catch (e) {}

module.exports = {};
