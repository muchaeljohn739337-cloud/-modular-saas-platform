name: Deprecated - DO auto deploy removed

on:
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated DO auto-deploy; use Render deploy instead."
