name: Deprecated - DO droplet workflow removed

# This workflow has been intentionally deprecated and left as a no-op.
# Deployments are handled by Render via .github/workflows/deploy.yml

on:
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated DO workflow; use Render deploy instead."
