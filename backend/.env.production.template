# ==============================================
# ADVANCIA PAY - PRODUCTION ENVIRONMENT
# ==============================================
# ⚠️  SECURITY WARNING: Generate NEW secrets for production!
# ⚠️  NEVER commit this file with real values to git!

# SERVER
NODE_ENV=production
PORT=4000
FRONTEND_URL=https://your-domain.com

# DATABASE (Use Render PostgreSQL URL)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# AUTHENTICATION
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=<GENERATE_NEW_64_CHAR_SECRET>
JWT_REFRESH_SECRET=<GENERATE_NEW_64_CHAR_SECRET>
SESSION_SECRET=<GENERATE_NEW_64_CHAR_SECRET>
OTP_SECRET=<GENERATE_NEW_32_CHAR_SECRET>

# STRIPE (LIVE KEYS!)
# ⚠️  Use LIVE keys (sk_live_..., pk_live_..., whsec_live_...)
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_WEBHOOK_SECRET=whsec_live_...

# CRYPTOMUS (Production)
CRYPTOMUS_API_KEY=<YOUR_PRODUCTION_API_KEY>
CRYPTOMUS_MERCHANT_ID=<YOUR_PRODUCTION_MERCHANT_ID>

# EMAIL (Gmail for OTP - use App Password)
# Get App Password: Google Account → Security → 2FA → App passwords
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=<GMAIL_APP_PASSWORD_16_CHARS>
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# WALLET SECURITY (CRITICAL!)
# ⚠️  NEVER share these! Store in password manager!
# Generate WALLET_ENCRYPTION_KEY: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WALLET_ENCRYPTION_KEY=<AES_256_KEY_32_BYTES_HEX>
WALLET_MASTER_SEED=<SECURE_BIP39_MNEMONIC_24_WORDS>

# ADMIN WALLETS (Your actual addresses)
ADMIN_BTC_WALLET_ADDRESS=bc1q...
ADMIN_ETH_WALLET_ADDRESS=0x...
ADMIN_USDT_WALLET_ADDRESS=0x...

# REDIS (Render Redis URL or disable)
REDIS_URL=redis://default:password@hostname:6379

# MONITORING
SENTRY_DSN=https://...@sentry.io/...
LOG_LEVEL=info

# Backups: handled via GitHub Actions upload to Cloudflare R2.
# No AWS vars needed in backend runtime.

# PUSH NOTIFICATIONS
VAPID_PUBLIC_KEY=<YOUR_VAPID_PUBLIC>
VAPID_PRIVATE_KEY=<YOUR_VAPID_PRIVATE>

# OPTIONAL (disable if not using)
RABBITMQ_URL=  # Leave empty to disable

# NOWPAYMENTS (if using)
NOWPAYMENTS_API_KEY=<YOUR_NOWPAYMENTS_API_KEY>
